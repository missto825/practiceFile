const startButton = document.querySelector(".start_btn");

shareButton.addEventLisener('click',copyUrl)
openButton.addEventLisener('click',openUrl)
closeButton.addEventLisener('click',closeUrl)
startButton.addEventLisener('click',calculatorUrl)